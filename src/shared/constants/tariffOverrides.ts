import type { TariffOverrides } from '../types/tariffOverrides'

export const EMPTY_TARIFF_OVERRIDES: TariffOverrides = {
  modalityPrices: {},
  discountPercentages: {},
  extraPrices: {},
  monthlyPremiumRates: {},
}
