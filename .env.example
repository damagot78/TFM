# PIN de cada agente (4 dígitos), leído por api/validate-pin.ts en el servidor.
# Nunca usar el prefijo VITE_ para estas claves: eso las empaquetaría en el
# cliente. Configurar los valores reales como variables de entorno en Vercel
# (Project Settings → Environment Variables), nunca en un archivo .env
# versionado en git.
PIN_AGENT_1=
PIN_AGENT_2=
PIN_AGENT_3=
PIN_AGENT_4=
